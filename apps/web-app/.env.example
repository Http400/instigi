# Base URL of the auth-service REST API (without the /api/auth suffix).
VITE_API_URL=http://localhost:4000
# Base URL of the training-service REST API (without the /api/exercises suffix).
VITE_TRAINING_API_URL=http://localhost:4001
